---
const repoUrl = 'https://github.com/tame-gg/SilkMC';
const releaseUrl = 'https://github.com/tame-gg/SilkMC/releases/latest';
const paperclipUrl = 'https://github.com/tame-gg/SilkMC/releases/download/v0.1.1-alpha/silkmc-paperclip-26.1.2.local-SNAPSHOT.jar';
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SilkMC - Region-Threaded Minecraft Server Software</title>
  <meta name="description" content="SilkMC is an alpha Folia-derived Minecraft server platform focused on regional multithreading, practical plugin compatibility, and visible safety warnings." />

  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://koels.site/silkmc" />
  <meta property="og:title" content="SilkMC - Region-Threaded Minecraft Server Software" />
  <meta property="og:description" content="An alpha Folia fork focused on preserving regional multithreading while improving Paper, Bukkit, and Spigot plugin compatibility." />
  <meta property="og:site_name" content="SilkMC" />
  <meta property="og:image" content="https://koels.site/og.png" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:image" content="https://koels.site/og.png" />
  <link rel="canonical" href="https://koels.site/silkmc" />

  <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'%3E%3Crect width='32' height='32' rx='8' fill='%23020810'/%3E%3Ccircle cx='16' cy='16' r='11' fill='none' stroke='%230060b0' stroke-width='1' opacity='0.5'/%3E%3Ccircle cx='16' cy='16' r='7' fill='none' stroke='%2340c8ff' stroke-width='1.2' opacity='0.75'/%3E%3Ccircle cx='16' cy='16' r='3' fill='%2340c8ff'/%3E%3Cpath d='M16 5 L16 11 M16 21 L16 27 M5 16 L11 16 M21 16 L27 16' stroke='%2340c8ff' stroke-width='1.2' stroke-linecap='round' opacity='0.6'/%3E%3C/svg%3E">

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet" />

  <style>
    @view-transition { navigation: auto; }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #020810;
      --bg2: #040e1e;
      --surface: rgba(2,12,28,0.6);
      --border: rgba(0,120,200,0.14);
      --border2: rgba(64,200,255,0.22);
      --text: #c8f0ff;
      --muted: rgba(200,240,255,0.55);
      --dim: rgba(200,240,255,0.3);
      --cyan: #40c8ff;
      --teal: #00ffcc;
      --red: #ff6b7a;
      --amber: #ffbd4a;
      --ease: cubic-bezier(0.16,1,0.3,1);
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'Inter', sans-serif;
      line-height: 1.6;
      min-height: 100vh;
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
    }

    body::before {
      content: '';
      position: fixed; inset: 0;
      background-image: radial-gradient(circle, rgba(255,255,255,0.018) 1px, transparent 1px);
      background-size: 28px 28px;
      pointer-events: none; z-index: 0;
    }

    body::after {
      content: ''; position: fixed; inset: 0; pointer-events: none; z-index: 9990;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.025'/%3E%3C/svg%3E");
    }

    .aurora { position: fixed; inset: 0; pointer-events: none; z-index: 0; overflow: hidden; }
    .ab { position: absolute; border-radius: 50%; filter: blur(120px); }
    .ab1 { width:700px;height:520px;opacity:.24;background:radial-gradient(ellipse,#0050c8 0%,transparent 70%);top:-120px;left:-160px;animation:ab1 40s ease-in-out infinite; }
    .ab2 { width:650px;height:650px;opacity:.16;background:radial-gradient(ellipse,#003080 0%,transparent 70%);top:-80px;right:-140px;animation:ab2 52s ease-in-out infinite; }
    .ab3 { width:520px;height:420px;opacity:.18;background:radial-gradient(ellipse,#00a8b0 0%,transparent 70%);bottom:20%;left:25%;animation:ab3 30s ease-in-out infinite; }
    .ab4 { width:480px;height:480px;opacity:.14;background:radial-gradient(ellipse,#0060b0 0%,transparent 70%);bottom:0;right:8%;animation:ab4 55s ease-in-out infinite; }
    @keyframes ab1{0%,100%{transform:translate(0,0) scale(1)}25%{transform:translate(80px,60px) scale(1.1)}50%{transform:translate(20px,120px) scale(.93)}75%{transform:translate(-40px,40px) scale(1.05)}}
    @keyframes ab2{0%,100%{transform:translate(0,0) scale(1)}30%{transform:translate(-90px,70px) scale(1.08)}60%{transform:translate(-30px,140px) scale(.91)}80%{transform:translate(50px,60px) scale(1.04)}}
    @keyframes ab3{0%,100%{transform:translate(0,0) scale(1)}35%{transform:translate(60px,-80px) scale(1.12)}70%{transform:translate(-50px,-40px) scale(.9)}}
    @keyframes ab4{0%,100%{transform:translate(0,0) scale(1)}40%{transform:translate(-70px,-90px) scale(1.1)}70%{transform:translate(30px,-60px) scale(.94)}}

    nav, main, footer, .stats-strip { position: relative; z-index: 2; }

    nav {
      position: sticky; top: 0; z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 2.5rem; height: 58px;
      background: rgba(2,8,16,0.75);
      backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px);
      border-bottom: 1px solid var(--border);
    }

    .logo-dot {
      width: 7px; height: 7px; border-radius: 50%;
      background: var(--teal);
      box-shadow: 0 0 8px var(--teal), 0 0 16px rgba(0,255,204,0.4);
      animation: dot-pulse 2s ease infinite;
      flex-shrink: 0;
    }
    @keyframes dot-pulse{0%,100%{box-shadow:0 0 6px var(--teal)}50%{box-shadow:0 0 20px var(--teal),0 0 40px rgba(0,255,204,0.2)}}

    .nav-right { display: flex; align-items: center; gap: 2rem; }
    .nav-links { display: flex; gap: 1.75rem; list-style: none; }
    .nav-links a { color: var(--muted); text-decoration: none; font-size: 0.82rem; transition: color 0.15s; }
    .nav-links a:hover { color: var(--text); }

    .nav-gh {
      display: inline-flex; align-items: center; gap: 0.4rem;
      padding: 0.38rem 0.9rem; border-radius: 6px;
      border: 1px solid var(--border2); background: var(--surface);
      color: var(--muted); text-decoration: none;
      font-size: 0.8rem; font-weight: 500;
      transition: border-color 0.2s, color 0.2s;
    }
    .nav-gh:hover { border-color: var(--cyan); color: var(--text); }

    .back-link {
      display: flex; align-items: center; gap: 0.5rem;
      font-family: 'JetBrains Mono', monospace; font-size: 0.68rem;
      letter-spacing: 0.12em; text-transform: uppercase;
      color: var(--dim); text-decoration: none;
      transition: color 0.15s;
    }
    .back-link:hover { color: var(--cyan); }

    .hero {
      position: relative; z-index: 2;
      padding: 4.8rem 2.5rem 4rem;
      max-width: 1180px; margin: 0 auto;
      animation: fade-up 0.8s var(--ease) both;
    }

    .launch-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.15fr) minmax(330px, 0.85fr);
      gap: 14px;
      align-items: stretch;
    }

    .hero-card,
    .panel-card {
      background: rgba(2,12,28,0.55);
      border: 1px solid var(--border);
      border-radius: 14px;
      backdrop-filter: blur(16px);
      position: relative;
      overflow: hidden;
    }

    .hero-card::before,
    .panel-card::before {
      content: '';
      position: absolute; top: 0; left: 0; right: 0; height: 1px;
      background: linear-gradient(90deg, transparent, var(--cyan), transparent);
      opacity: .65;
    }

    .hero-card {
      min-height: 440px;
      padding: clamp(2rem, 5vw, 4rem);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .badge {
      display: inline-flex; align-items: center; gap: 0.5rem;
      align-self: flex-start;
      padding: 0.28rem 0.9rem; border-radius: 99px;
      border: 1px solid rgba(64,200,255,0.22);
      background: rgba(64,200,255,0.06);
      color: var(--teal); font-size: 0.72rem; font-weight: 500;
      font-family: 'JetBrains Mono', monospace;
      letter-spacing: 0.06em; white-space: nowrap;
    }

    .badge-dot {
      width: 5px; height: 5px; border-radius: 50%;
      background: var(--teal); animation: blink 1.4s ease infinite;
    }
    @keyframes blink{0%,100%{opacity:1}50%{opacity:.15}}

    h1 {
      font-size: clamp(2.8rem, 5.3vw, 4.85rem);
      font-weight: 800;
      line-height: 0.98;
      letter-spacing: -0.04em;
      color: #e8f8ff;
      margin: 1.35rem 0 1.1rem;
      max-width: 780px;
    }

    h1 .accent {
      background: linear-gradient(135deg, var(--cyan) 0%, var(--teal) 60%, var(--cyan) 100%);
      background-size: 200% auto;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: shimmer 5s linear infinite;
    }
    @keyframes shimmer { to { background-position: 200% center; } }

    .hero-sub {
      font-size: 1rem;
      color: var(--muted);
      line-height: 1.75;
      max-width: 640px;
    }

    .inline-alert {
      display: inline-flex;
      margin-top: 1rem;
      padding: 0.55rem 0.75rem;
      border: 1px solid rgba(255,107,122,0.32);
      border-radius: 8px;
      background: rgba(255,107,122,0.06);
      color: rgba(255,216,222,0.9);
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.66rem;
      line-height: 1.55;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    .hero-actions {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      margin-top: 1.4rem;
    }

    .btn-primary,
    .btn-ghost {
      display: inline-flex; align-items: center; gap: 0.5rem;
      padding: 0.75rem 1.6rem; border-radius: 7px;
      font-family: 'JetBrains Mono', monospace; font-size: 0.72rem;
      font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase;
      text-decoration: none; transition: 0.2s var(--ease);
    }

    .btn-primary {
      background: var(--cyan); color: #010810;
      border: 1px solid var(--cyan);
      box-shadow: 0 0 28px rgba(64,200,255,0.25);
    }
    .btn-primary:hover { background: #2ab0e8; border-color: #2ab0e8; transform: translateY(-1px); }

    .btn-ghost {
      background: transparent; color: var(--muted);
      border: 1px solid var(--border2);
    }
    .btn-ghost:hover { color: #e8f8ff; border-color: rgba(64,200,255,0.35); background: rgba(0,120,200,0.08); }

    .panel-stack { display: grid; gap: 14px; }

    .panel-card { padding: 1.4rem; }
    .panel-title {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.68rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--cyan);
      margin-bottom: 1rem;
    }

    .alpha-card {
      border-color: rgba(255,107,122,0.32);
      background: rgba(32,7,18,0.42);
    }
    .alpha-card::before { background: linear-gradient(90deg, transparent, var(--red), transparent); }
    .alpha-card .panel-title { color: var(--red); }
    .alpha-card p {
      color: rgba(255,216,222,0.82);
      font-size: 0.92rem;
      line-height: 1.75;
    }

    .terminal-card {
      padding: 0;
      background: rgba(2,5,12,0.85);
      box-shadow:
        0 40px 90px rgba(0,0,0,0.55),
        0 0 0 1px rgba(64,200,255,0.04),
        inset 0 1px 0 rgba(255,255,255,0.025);
    }

    .terminal-bar {
      display: flex; align-items: center;
      padding: 0.65rem 1.1rem;
      background: rgba(255,255,255,0.022);
      border-bottom: 1px solid rgba(64,200,255,0.08);
      gap: 0.5rem;
    }

    .terminal-dots { display: flex; gap: 5px; flex-shrink: 0; }
    .terminal-dots span { width: 9px; height: 9px; border-radius: 50%; }
    .terminal-dots span:nth-child(1) { background: rgba(255,95,86,0.4); }
    .terminal-dots span:nth-child(2) { background: rgba(255,189,46,0.4); }
    .terminal-dots span:nth-child(3) { background: rgba(39,201,63,0.4); }

    .terminal-filename {
      flex: 1; text-align: center;
      font-family: 'JetBrains Mono', monospace; font-size: 0.67rem;
      letter-spacing: 0.1em; color: var(--dim);
    }

    .terminal-body {
      padding: 1.15rem 1.4rem;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.78rem; line-height: 1.9;
    }

    .t-line { display: block; }
    .t-gap { display: block; height: 0.55rem; }
    .t-comment { color: rgba(200,240,255,0.22); }
    .t-section { color: var(--cyan); }
    .t-key { color: var(--text); }
    .t-val { color: var(--teal); }
    .t-warn {
      display: block;
      margin: 0.45rem -1.4rem 0;
      padding: 0.35rem 1.4rem;
      border-left: 2px solid var(--red);
      background: rgba(255,107,122,0.08);
      color: rgba(255,216,222,0.88);
    }

    .mini-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
    }

    .mini {
      background: rgba(2,12,28,0.55);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 0.85rem 1rem;
      display: flex;
      flex-direction: column;
      gap: 0.2rem;
    }

    .mini-val {
      font-family: 'JetBrains Mono', monospace;
      font-size: 1.05rem;
      font-weight: 700;
      color: #e8f8ff;
      line-height: 1;
    }
    .mini-val.c { color: var(--cyan); }
    .mini-val.g { color: var(--teal); }
    .mini-val.r { color: var(--red); }
    .mini-label {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.58rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--dim);
    }

    .stats-strip {
      display: flex; justify-content: center;
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
    }

    .stat {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 0.25rem;
      padding: 1.8rem 1rem;
      border-right: 1px solid var(--border);
    }
    .stat:last-child { border-right: none; }

    .stat-value {
      font-size: 1.9rem;
      font-weight: 900;
      letter-spacing: -0.04em;
      line-height: 1;
      color: #e8f8ff;
    }
    .stat-value.c { color: var(--cyan); text-shadow: 0 0 32px rgba(64,200,255,0.3); }
    .stat-value.g { color: var(--teal); text-shadow: 0 0 28px rgba(0,255,204,0.25); }
    .stat-value.r { color: var(--red); text-shadow: 0 0 28px rgba(255,107,122,0.18); }

    .stat-label {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.63rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--dim);
      text-align: center;
    }

    .section {
      padding: 6rem 1.5rem;
      max-width: 1080px;
      margin: 0 auto;
    }

    .section-tag {
      display: block;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.68rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--cyan);
      margin-bottom: 1rem;
    }

    .section h2 {
      font-size: clamp(1.8rem, 3.5vw, 2.8rem);
      font-weight: 800;
      letter-spacing: -0.03em;
      color: #e8f8ff;
      margin-bottom: 3rem;
      line-height: 1.2;
    }

    .bento {
      display: grid;
      grid-template-columns: repeat(6, 1fr);
      gap: 12px;
    }

    .card {
      background: rgba(2,12,28,0.55);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 1.8rem;
      backdrop-filter: blur(16px);
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      position: relative;
      overflow: hidden;
      transition: border-color 0.25s, transform 0.25s var(--ease), box-shadow 0.25s;
    }
    .card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--cyan), transparent);
      opacity: 0;
      transition: opacity 0.3s;
    }
    .card:hover { border-color: var(--border2); transform: translateY(-3px); box-shadow: 0 16px 48px rgba(0,0,0,0.4); }
    .card:hover::before { opacity: 1; }

    .c2 { grid-column: span 2; }
    .c3 { grid-column: span 3; }
    .c6 { grid-column: span 6; }

    .card-title {
      font-size: 1rem;
      font-weight: 700;
      letter-spacing: -0.01em;
      color: #e8f8ff;
    }

    .card-body {
      font-size: 0.875rem;
      color: var(--muted);
      line-height: 1.65;
      flex: 1;
    }

    .card-chip {
      display: inline-flex;
      align-self: flex-start;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.6rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      padding: 3px 9px;
      border-radius: 99px;
      color: var(--teal);
      border: 1px solid rgba(0,255,204,0.3);
      background: rgba(0,255,204,0.06);
    }
    .card-chip.blue { color: var(--cyan); border-color: rgba(64,200,255,0.3); background: rgba(64,200,255,0.06); }
    .card-chip.red { color: var(--red); border-color: rgba(255,107,122,0.3); background: rgba(255,107,122,0.06); }

    .steps {
      display: flex;
      flex-direction: column;
      gap: 0;
      margin-top: 3rem;
    }

    .step {
      display: flex;
      align-items: flex-start;
      gap: 1.5rem;
      padding: 1.5rem 0;
      border-bottom: 1px solid var(--border);
    }
    .step:first-child { border-top: 1px solid var(--border); }

    .step-num {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.65rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--cyan);
      flex-shrink: 0;
      padding-top: 0.15rem;
      min-width: 32px;
    }

    .step-title {
      font-size: 0.95rem;
      font-weight: 600;
      color: #e8f8ff;
      margin-bottom: 0.35rem;
    }
    .step-desc {
      font-size: 0.85rem;
      color: var(--muted);
      line-height: 1.6;
    }

    .code-block {
      display: block;
      margin-top: 0.75rem;
      padding: 0.85rem 1.1rem;
      border-radius: 8px;
      background: rgba(0,0,0,0.35);
      border: 1px solid rgba(64,200,255,0.1);
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.78rem;
      color: var(--teal);
      line-height: 1.55;
      white-space: pre;
      overflow-x: auto;
    }

    footer {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1rem;
      padding: 2.5rem 1.5rem;
      text-align: center;
      border-top: 1px solid var(--border);
      position: relative;
      z-index: 2;
    }

    .footer-logo {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-family: 'JetBrains Mono', monospace;
      font-weight: 700;
      font-size: 0.72rem;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--muted);
      text-decoration: none;
    }

    .footer-links { display: flex; gap: 1.75rem; }
    .footer-links a { font-size: 0.78rem; color: var(--dim); text-decoration: none; transition: color 0.15s; }
    .footer-links a:hover { color: var(--text); }

    .footer-copy { font-size: 0.72rem; color: var(--dim); max-width: 860px; }

    @keyframes fade-up { from{opacity:0;transform:translateY(24px)} to{opacity:1;transform:translateY(0)} }
    .reveal { opacity: 0; transform: translateY(20px); transition: opacity 0.7s var(--ease), transform 0.7s var(--ease); }
    .reveal.visible { opacity: 1; transform: translateY(0); }

    @media(max-width: 980px) {
      .launch-grid { grid-template-columns: 1fr; }
      .hero-card { min-height: auto; }
    }

    @media(max-width: 860px) {
      .c2, .c3, .c6 { grid-column: span 6; }
    }

    @media(max-width: 700px) {
      nav { padding: 0 1.25rem; }
      .nav-links { display: none; }
      .hero { padding: 5rem 1.25rem 4rem; }
      h1 { font-size: clamp(2.65rem, 13vw, 4rem); }
      .hero-actions { flex-direction: column; }
      .btn-primary, .btn-ghost { justify-content: center; }
      .stats-strip { flex-wrap: wrap; }
      .stat { min-width: 50%; border-right: none; border-bottom: 1px solid var(--border); }
      .section { padding: 4rem 1.25rem; }
      .footer-links { gap: 1.25rem; flex-wrap: wrap; justify-content: center; }
    }
  </style>
</head>
<body>

<div class="aurora" aria-hidden="true">
  <div class="ab ab1"></div><div class="ab ab2"></div>
  <div class="ab ab3"></div><div class="ab ab4"></div>
</div>

<nav>
  <a href="/" class="back-link">
    <svg width="12" height="12" viewBox="0 0 12 12" fill="none" style="flex-shrink:0">
      <path d="M8 2L4 6L8 10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
    koels.site
  </a>
  <div class="nav-right">
    <ul class="nav-links">
      <li><a href="#status">Status</a></li>
      <li><a href="#compat">Compatibility</a></li>
      <li><a href="#setup">Setup</a></li>
    </ul>
    <a class="nav-gh" href={repoUrl} target="_blank" rel="noopener">
      <svg width="14" height="14" viewBox="0 0 16 16" fill="currentColor"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg>
      GitHub
    </a>
  </div>
</nav>

<main>
  <section class="hero">
    <div class="launch-grid">
      <div class="hero-card">
        <div>
          <div class="badge">
            <span class="badge-dot"></span>
            <span id="badge-text">v0.1.1-alpha - Folia 26.1.2</span>
          </div>
          <h1>Threaded regions, <span class="accent">plugin compatibility work.</span></h1>
          <p class="hero-sub">
            SilkMC keeps Folia's regional multithreading architecture while narrowing the rough edge for Paper, Bukkit, and Spigot plugins.
            It is meant to make failures clearer, startup safer, and testing less mysterious.
          </p>
        </div>
        <div>
          <div class="hero-actions">
            <a class="btn-primary" id="download-btn" href={paperclipUrl}>Download paperclip jar</a>
            <a class="btn-ghost" href={releaseUrl} target="_blank" rel="noopener">Latest release</a>
          </div>
          <p class="inline-alert">Alpha: bugs, memory leaks, security issues, and data loss are possible.</p>
        </div>
      </div>

      <div class="panel-stack">
        <div class="panel-card alpha-card">
          <div class="panel-title">Alpha warning</div>
          <p>
            SilkMC is alpha software. Bugs, memory leaks, security issues, crashes, data loss, plugin breakage,
            and unstable behavior are possible. Test with disposable worlds before trusting it anywhere important.
          </p>
        </div>

        <div class="panel-card terminal-card">
          <div class="terminal-bar">
            <div class="terminal-dots"><span></span><span></span><span></span></div>
            <span class="terminal-filename">silkmc-status.yml</span>
          </div>
          <div class="terminal-body">
            <span class="t-line t-comment"># release channel</span>
            <span class="t-line"><span class="t-key">version</span>: <span class="t-val">v0.1.1-alpha</span></span>
            <span class="t-line"><span class="t-key">runtime</span>: <span class="t-val">JDK 25</span></span>
            <span class="t-line"><span class="t-key">artifact</span>: <span class="t-val">paperclip</span></span>
            <span class="t-warn">risk: alpha bugs, leaks, security issues</span>
          </div>
        </div>

        <div class="mini-grid">
          <div class="mini">
            <span class="mini-val c">Folia</span>
            <span class="mini-label">regional base</span>
          </div>
          <div class="mini">
            <span class="mini-val r">Alpha</span>
            <span class="mini-label">not hardened</span>
          </div>
          <div class="mini">
            <span class="mini-val g">Paper</span>
            <span class="mini-label">plugin target</span>
          </div>
          <div class="mini">
            <span class="mini-val">JDK 25</span>
            <span class="mini-label">runtime</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="stats-strip" id="status">
    <div class="stat">
      <span class="stat-value c" id="release-name">v0.1.1</span>
      <span class="stat-label">latest public release</span>
    </div>
    <div class="stat">
      <span class="stat-value g">safe</span>
      <span class="stat-label">failures should be clearer</span>
    </div>
    <div class="stat">
      <span class="stat-value r">alpha</span>
      <span class="stat-label">bugs and security risks possible</span>
    </div>
    <div class="stat">
      <span class="stat-value">GPL-3.0</span>
      <span class="stat-label">open source</span>
    </div>
  </div>

  <section class="section" id="compat">
    <span class="section-tag">Compatibility stance</span>
    <h2>Closer to Paper plugin expectations without pretending Folia is single-threaded.</h2>

    <div class="bento">
      <div class="card c2 reveal">
        <div class="card-title">Metadata-aware startup</div>
        <p class="card-body">Plugins can opt into SilkMC compatibility while Folia-aware metadata remains honored for upstream interoperability.</p>
        <span class="card-chip">plugin loading</span>
      </div>
      <div class="card c2 reveal" style="transition-delay:.05s">
        <div class="card-title">Scheduler bridging</div>
        <p class="card-body">Legacy task assumptions are handled where the server can preserve correctness across region-threaded execution.</p>
        <span class="card-chip blue">thread ownership</span>
      </div>
      <div class="card c2 reveal" style="transition-delay:.1s">
        <div class="card-title">Visible unsafe edges</div>
        <p class="card-body">When behavior cannot be made safe, SilkMC should warn, disable, or fail loudly instead of hiding a corruption risk.</p>
        <span class="card-chip red">alpha guardrails</span>
      </div>

      <div class="card c3 reveal" style="transition-delay:.14s">
        <div class="card-title">What it is trying to improve</div>
        <p class="card-body">Paper, Bukkit, and Spigot plugin startup paths; lifecycle timing; scheduler assumptions; async misuse diagnostics; and more readable incompatibility reports.</p>
      </div>
      <div class="card c3 reveal" style="transition-delay:.18s">
        <div class="card-title">What it is not promising</div>
        <p class="card-body">It is not a guarantee that every plugin works, and it is not production-hardened. Bugs, memory leaks, security issues, and data loss remain possible.</p>
      </div>
    </div>
  </section>

  <section class="section" id="setup" style="border-top:1px solid var(--border)">
    <span class="section-tag">Setup path</span>
    <h2>Use the runnable paperclip jar and test plugins in layers.</h2>

    <div class="steps">
      <div class="step reveal">
        <span class="step-num">01</span>
        <div>
          <div class="step-title">Build the project</div>
          <p class="step-desc">Apply patches first, then build the server and paperclip package.</p>
          <code class="code-block">git clone https://github.com/tame-gg/SilkMC.git
cd SilkMC
./gradlew applyAllPatches
./gradlew build
./gradlew :silkmc-server:createPaperclipJar</code>
        </div>
      </div>

      <div class="step reveal" style="transition-delay:.06s">
        <span class="step-num">02</span>
        <div>
          <div class="step-title">Run paperclip, not the module jar</div>
          <p class="step-desc">The plain silkmc-server jar is not meant to be launched standalone.</p>
          <code class="code-block">java -Xms4G -Xmx4G -jar silkmc-server/build/libs/silkmc-paperclip-26.1.2.local-SNAPSHOT.jar nogui</code>
        </div>
      </div>

      <div class="step reveal" style="transition-delay:.12s">
        <span class="step-num">03</span>
        <div>
          <div class="step-title">Treat warnings as test data</div>
          <p class="step-desc">Add plugins gradually, keep logs from each run, and test on disposable worlds because alpha bugs, leaks, and security issues may still exist.</p>
        </div>
      </div>
    </div>
  </section>
</main>

<footer>
  <a href="/" class="footer-logo">
    <span class="logo-dot"></span>
    Koels.site
  </a>
  <div class="footer-links">
    <a href="/">Home</a>
    <a href={repoUrl} target="_blank" rel="noopener">GitHub</a>
    <a href="https://github.com/tame-gg/SilkMC/releases" target="_blank" rel="noopener">Releases</a>
    <a href="https://github.com/tame-gg/SilkMC/blob/main/README.md" target="_blank" rel="noopener">Docs</a>
  </div>
  <span class="footer-copy">SilkMC is alpha software derived from Folia and Paper. Bugs, memory leaks, security vulnerabilities, data loss, and plugin incompatibilities are possible.</span>
</footer>

<script is:inline>
if ('scrollRestoration' in history) history.scrollRestoration = 'manual';
window.scrollTo(0, 0);

const obs = new IntersectionObserver(
  es => es.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); }),
  { threshold: 0.08 }
);
document.querySelectorAll('.reveal').forEach(el => obs.observe(el));

(async () => {
  try {
    const res = await fetch('https://api.github.com/repos/tame-gg/SilkMC/releases/latest');
    if (!res.ok) return;
    const data = await res.json();
    const tag = data.tag_name || 'v0.1.1-alpha';
    const badge = document.getElementById('badge-text');
    const release = document.getElementById('release-name');
    if (badge) badge.textContent = tag + ' - Folia 26.1.2';
    if (release) release.textContent = tag.replace('-alpha', '');
    const jar = (data.assets || []).find(a => /paperclip.*\.jar$/i.test(a.name));
    const btn = document.getElementById('download-btn');
    if (btn && jar) {
      btn.href = jar.browser_download_url;
      btn.removeAttribute('target');
      btn.removeAttribute('rel');
      btn.setAttribute('download', jar.name);
    }
  } catch (_) {}
})();
</script>

</body>
</html>
